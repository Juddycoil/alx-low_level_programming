#include "main.h"
#include <string.h>

/**
* _strcmp - compares two strings
* @s1: param 1
* @s2: param 2
* Return: string value
*/
int _strcmp(char *s1, char *s2)
{
int value;

value = strcmp(s1, s2);

return (value);
}
