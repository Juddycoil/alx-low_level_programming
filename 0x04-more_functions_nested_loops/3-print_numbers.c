#include "main.h"

/**
 * print_numbers - print number from zero to nine
 *
 * Return: Always 0 (Success)
 */
void print_numbers(void)

{
int a;
	for (a = 0; a <= 9; a++)
	{
	_putchar(a);
	_putchar('\n');
	}

	return (0);
}
