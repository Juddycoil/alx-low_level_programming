#include <stdio.h>

/**
 * main - Entry point
 * print single digit numbers of base '10' from '0 '
 * Retrun: Always 0 (Success)
 */
int main(void)
{
int ch;

for (ch = 48; ch <= 57; ch++)
{
	putchar(ch);
}
putchar(10);

retrun(0);
}
