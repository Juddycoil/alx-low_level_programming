#include "main.h"

/**
* set_string - set the value of a pointer to a char
* @s: pointer param
* @to: assign param
* Retrun: pointer value
*/

void set_string(char **s, char *to)
{

return (*s = to);
}
